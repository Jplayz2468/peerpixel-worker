#!/usr/bin/env bash
# Kept for the people and the pages that still say to run this. The app
# installs itself now; there is nothing here that PeerPixel.command does not do.
exec "$(dirname "$0")/launch/bootstrap.sh" "$@"
